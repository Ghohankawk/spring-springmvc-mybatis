<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pioneer.mapper.BaseMapper" >
    <insert id="insertSql" parameterType="java.lang.String" >
        ${value}
    </insert>
    <update id="updateSql" parameterType="java.lang.String" >
        ${value}
    </update>
    <delete id="deleteSql" parameterType="java.lang.String" >
        ${value}
    </delete>
    <select id="selectSql" parameterType="java.lang.String" resultType="java.lang.String" >
        ${value}
    </select>
    <select id="selectMapSql" parameterType="java.lang.String" resultType="map" >
        ${value}
    </select>
    <select id="selectListMapSql" parameterType="java.lang.String" resultType="map" >
        ${value}
    </select>
</mapper>